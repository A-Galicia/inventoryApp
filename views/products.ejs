<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Food Inventory</title>
    <link rel="stylesheet" href="welcome.css" />
  </head>
  <body>
    <div class="main">
      <h1>All Products</h1>
      <ul>
        <% products.map((product) => { %>
        <li>Product: <%= product.name %> -<%= product.price %></li>
        <% }) %>
      </ul>
    </div>
  </body>
</html>
